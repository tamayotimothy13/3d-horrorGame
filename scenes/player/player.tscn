[gd_scene format=3 uid="uid://dky2rjs4ef1ri"]

[ext_resource type="PackedScene" uid="uid://vva8r34hd3yy" path="res://scenes/objects/flashlight_1.tscn" id="3_lvxji"]

[sub_resource type="GDScript" id="GDScript_gmlin"]
script/source = "extends CharacterBody3D

const SPEED = 18
const JUMP_VELOCITY = 25

var gravity = ProjectSettings.get_setting(\"physics/3d/default_gravity\")

@onready var camera = $Camera3D
@onready var flashlight_1: SpotLight3D = $Camera3D/flashlight_1

func _ready():
	Input.set_mouse_mode(Input.MOUSE_MODE_CAPTURED)

func _unhandled_input(event):
	if event is InputEventMouseMotion:
		rotate_y(-event.relative.x * 0.005)
		camera.rotate_x(-event.relative.y * 0.005)
		camera.rotation.x = clamp(camera.rotation.x, deg_to_rad(-80), deg_to_rad(80))
	if event.is_action_pressed(\"ui_cancel\"):
		Input.set_mouse_mode(Input.MOUSE_MODE_VISIBLE)

func _physics_process(delta):
	if not is_on_floor():
		velocity.y -= gravity * delta
	
	# handle jump
	if Input.is_action_just_pressed(\"jump\") and is_on_floor():
		velocity.y = JUMP_VELOCITY 
		
	# Make sure you mapped these actions in Project Settings -> Input Map
	var input_dir = Input.get_vector(\"move_left\", \"move_right\", \"move_forward\", \"move_back\")
	var direction = (transform.basis * Vector3(input_dir.x, 0, input_dir.y)).normalized()
	
	if direction:
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)
		velocity.z = move_toward(velocity.z, 0, SPEED)

	move_and_slide()

func _on_trigger_zone_body_entered(body: Node3D) -> void:
	if body.is_in_group(\"player\"):
		print(\"Na-trigger ng Player ang zone!\")
		# Dito mo ilalagay yung code pang-spawn ng kalaban sa susunod
"

[sub_resource type="CapsuleShape3D" id="CapsuleShape3D_27gba"]

[node name="Player" type="CharacterBody3D" unique_id=1925318435 groups=["player"]]
script = SubResource("GDScript_gmlin")

[node name="CollisionShape3D" type="CollisionShape3D" parent="." unique_id=2139223559]
shape = SubResource("CapsuleShape3D_27gba")
debug_color = Color(0.8727141, 0.26935217, 0.5652857, 0.41960785)

[node name="Camera3D" type="Camera3D" parent="." unique_id=469751175]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.56890416, 0)

[node name="flashlight_1" parent="Camera3D" unique_id=2019525122 instance=ExtResource("3_lvxji")]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.06490773, 0)
distance_fade_enabled = true
spot_angle = 45.0
